import 'package:flutter/material.dart';

/// Classe contenant les couleurs principales de l'application Terre du Kai Fracturé
class KaiColors {
  // Couleur principale sombre (remplace l'orange)
  static const Color primaryDark = Color(0xFF0B0F1A);

  // Couleur d'accent (remplace le jaune)
  static const Color accent = Color(0xFF4AE3E2);

  // Couleurs additionnelles
  static const Color background = Color(0xFF121212);
  static const Color cardBackground = Color(0xFF1E1E1E);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;

  // Couleurs spécifiques
  static const Color error = Colors.red;
  static const Color success = Colors.green;
}
